{
  "name": "Taxman",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy": "npm run build && surge -d https://taxman.surge.sh dist"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^1.0.0-next.7",
    "surge": "^0.23.0",
    "svelte": "^3.37.0",
    "vite": "^2.3.3",
    "vite-plugin-pwa": "^0.7.3"
  }
}
